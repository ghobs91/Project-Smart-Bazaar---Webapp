<li>
  <span class="content">
  	<%= micropost.keyword %>
  	$<%= micropost.min %>-
    $<%= micropost.max %>
  	<%= micropost.condition %>
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  </span>
  <% if current_user?(micropost.user) %>
    <%= link_to "delete", micropost, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: micropost.keyword %>
  <% end %>
</li>